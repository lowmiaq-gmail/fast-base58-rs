# fast-base58-rs 指南 (Agent Guide)

## 1. 本层级核心职责

实现并验证 `base58==2.1.1` 的 Rust-backed 完整 drop-in replacement，同时保留 pure-Python fallback、完整 CLI 和 typing contract。

## 2. 下级目录递归映射 (Sub-Directory Map)

- `/upstream`: 冻结的 `keis/base58` v2.1.1 oracle、原始测试与 artifacts。
- `/python`: native mixed-project Python compatibility layer。
- `/fallback`: 与 native wheel 同版本的 pure-Python fallback。
- `/src`: Rust/PyO3 实现。
- `/tests`: Candidate contract tests。
- `/scripts`: differential、artifact、fresh-wheel 与 release gate。
- `/docs`: release、benchmark、SEO 与验证报告。
- `/.github`: packaged-wheel CI 与 guarded release workflows。

## 3. 运行、测试与输出规范

- **允许的入口文件**: `pyproject.toml`, `Cargo.toml`, `scripts/`, `.github/workflows/`。
- **临时数据存放点**: ignored `.venv/`, `target/`, `.pytest_cache/` 以及系统临时目录；immutable artifacts 的路径与 SHA256 必须写入验证报告。

## 4. 阶段性清理清单 (Cleanup Checklist)

- [ ] 检查并移除无用的临时导出文件。
- [ ] 确保无未跟踪的垃圾代码残留。

## 5. 渐进式发现日志 (Progressive Discovery Log)

- [2026-08-12] 锁定 `keis/base58` v2.1.1；现有 Rust `based58` 仅近似 API，缺少字符串输入、整数 API、autofix、原 namespace/CLI 等完整 contract，因此按 ADAPT 路径补全。
